# Sanity CMS Configuration
# Get these values from https://www.sanity.io/manage

# Your Sanity project ID (found in your project settings)
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id-here

# Your Sanity dataset name (usually "production" or "development")
NEXT_PUBLIC_SANITY_DATASET=production

# API version (use YYYY-MM-DD format)
NEXT_PUBLIC_SANITY_API_VERSION=2025-01-01

# Revalidation Webhook Secret (optional - for on-demand cache updates)
# Generate a secure random string and use the same value in your Sanity webhook
REVALIDATE_SECRET=your-secret-token-here

# AI Intake (optional - for /admin/intake profile extraction)
# Get API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# Semantic Search (optional - Step 14 pgvector + embeddings)
# Set to 1 to enable semantic search with vectors, 0 for keyword fallback
NEXT_PUBLIC_FEATURE_SEMANTIC_SEARCH=0

# Postgres with pgvector extension (e.g., Neon, Supabase, local Postgres)
DATABASE_URL=postgres://USER:PASS@HOST:PORT/DB

# OpenAI API key for embeddings (text-embedding-3-small)
OPENAI_API_KEY=sk-...

# Secret to protect /api/search/index endpoint
INDEX_SECRET=generate-a-long-random-string

# Share Pack + CRM (optional - Step 15)
# If set, /api/crm/lead will forward payloads to this webhook URL
CRM_WEBHOOK_URL=https://hooks.zapier.com/...
CRM_WEBHOOK_AUTH=Bearer xyz
 SANITY_API_TOKEN=skCbe5WGhYGjciagwJqO65xiErguwMihUHubIH5RJIyzqHJF2fsd5Ez9oKNK26tGBd1n8xsXLJOn0wKi99mEbXqhnS5hzqBOChiZiuZ3RGl5db1ns8IJj5aB0rLJXQGBfT89RLPFUbceZYnXuF97DQhUiZZ6mN1cpf224p41DI24Xp2jdr8v